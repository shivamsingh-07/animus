apiVersion: apps/v1
kind: Deployment
metadata:
  name: animus-admin
  labels:
    app: animus-admin
spec:
  replicas: {{ .Values.admin.replicas }}
  selector:
    matchLabels:
      app: animus-admin
  template:
    metadata:
      labels:
        app: animus-admin
    spec:
      containers:
        - name: animus-admin
          image: "{{ .Values.image.repository }}/animus-admin:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: API_UPSTREAM
              value: http://animus-api-svc:4000
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            {{- toYaml .Values.admin.resources | nindent 12 }}

---
apiVersion: v1
kind: Service
metadata:
  name: animus-admin-svc
  labels:
    app: animus-admin
spec:
  selector:
    app: animus-admin
  ports:
    - port: 80
      targetPort: 80
